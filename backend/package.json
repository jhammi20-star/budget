{
  "name": "budget-compass-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/handler.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.888.0",
    "@aws-sdk/lib-dynamodb": "^3.888.0"
  }
}
